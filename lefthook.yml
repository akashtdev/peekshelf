pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc,css}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true
    typecheck:
      run: bun run compile

pre-push:
  commands:
    build:
     run: CI=1 bun run build
